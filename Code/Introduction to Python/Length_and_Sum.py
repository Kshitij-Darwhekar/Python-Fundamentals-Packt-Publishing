# Length and SUm Function in Python
# len() --> Returns the length of an object
# sum() --> Sums the items of an iterator

grades = [80, 75, 90, 100]

total = sum(grades)
length = len(grades)

average = total / length
print("Average: ",average)
